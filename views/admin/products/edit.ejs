<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1,shrink-to-fit=no">
    <title>ITD Canada Students</title>
    <link href="../../../stylesheets/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <h1>Update Product</h1>
      <div class="row">
        <div class="span6 offset3">
          <form accept-charset="UTF-8" action="/admin/products/<%= product.id %>" class="edit_product" id="edit_product_<%= product.id %>" method="post">
            <div class="field">
              <label for="product_name">Name</label>
              <input id="product_name" name="name" type="text" value="<%= product.name %>" />
            </div>
            <div class="field">
              <label for="product_related_url">Related URL</label>
              <input id="product_related_url" name="related_url" type="text" value="<%= product.related_url %>" />
            </div>
            <div class="field">
              <label for="product_image_url">Image URL</label>
              <input id="product_image_url" name="image_url" type="text" value="<%= product.image_url %>" />
            </div>
            <div class="field">
              <label for="product_course_id">Course ID</label>
              <input id="product_course_id" name="course_id" type="text" value="<%= product.course_id %>" />
            </div>
            <input class="btn btn-large btn-primary" name="commit" type="submit" value="Save changes" />
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
